# Files/folders excluded from Vercel deploy bundle.
# NOTE: do NOT ignore all of scripts/ — scripts/gen-sw.mjs runs in the build
# (prebuild) to generate public/sw.js. Exclude the repo-only Python tools instead.
*.py
__pycache__/
*.md
!README.md
